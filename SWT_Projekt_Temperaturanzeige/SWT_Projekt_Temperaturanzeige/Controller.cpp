#include "Controller.h"
#include <iostream>
using namespace std;

void Controller::run(string url) {
	/*
	 * while (button(Beenden) nicht gedrückt) {
			  subject.do_update(url);
	   }	
	*/
	_sleep(60000);
}